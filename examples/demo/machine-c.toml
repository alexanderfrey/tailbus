# Machine C: runs daemon with "weather" and "currency" data agents
node_id = "machine-c"
coord_addr = "__COORD_IP__:8443"
advertise_addr = "__MY_IP__:9443"
listen_addr = ":9443"
socket_path = "/tmp/tailbusd.sock"
key_file = "/tmp/tailbusd-c.key"
