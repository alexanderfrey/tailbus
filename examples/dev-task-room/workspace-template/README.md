# Dev Task Room Fixture Workspace

This workspace is intentionally small.

It contains:
- `client.py` for HTTP client tasks
- `parser.py` for parser tasks
- `todo.py` for filtering tasks
- `tests/` with baseline unit tests

The live demo copies this template into `/tmp/devtaskroom-workspace` before each task run.
